language: php

matrix:
  include:
    - php: 5.4
      env: 
        - UUID_GENERATOR='rhumsaa/uuid ^2.0'
        - HTTP_CLIENT='guzzle/guzzle'
    - php: 5.4
      env: 
        - UUID_GENERATOR='ramsey/uuid'
        - HTTP_CLIENT='guzzlehttp/guzzle'

before_install:
  - composer selfupdate

install:
  - composer update
  - composer require $UUID_GENERATOR
  - composer require $HTTP_CLIENT

script:
  - vendor/bin/phpspec run --format=pretty spec/